language: python
build:
  ci:
    - sudo apt -y install python3.7.2
    - make configure-pip
    - make configure-cmake
    - make configure-clang6-1604
    - pip3 install mama --upgrade
    - sudo apt -y --allow-unauthenticated install libdw-dev gdb
    - mama build clang
    - mama test
