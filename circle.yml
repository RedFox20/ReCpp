machine:
  python:
    version: 3.6.1
dependencies:
  override:
    - sudo apt -y install libdw-dev
    - pip install --upgrade pip
    - sudo pip install mama
    - make configure-clang6-1404
compile:
  override:
    - mama build clang
test:
  override:
    - mama test
