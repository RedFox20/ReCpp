language: cpp
matrix:
  include:
    - os: linux
      addons:
        apt:
          update: false
          sources:
            - llvm-toolchain-trusty-5.0
          packages:
            - build-essential
            - clang-5.0
            - libc++-dev
            - libc++abi-dev
            - libdw-dev
      env:
        - COMPILER=clang
    # - os: linux
    #   addons:
    #     apt:
    #       update: false
    #       sources:
    #         - ubuntu-toolchain-r-test
    #       packages:
    #         - build-essential
    #         - g++-7
    #         - libstdc++-7-dev
    #         - libdw-dev
    #   env:
    #     - COMPILER=clang

install:
  - make configure-${COMPILER}

script: make ${COMPILER}

after_success: cd build && ./Tests
