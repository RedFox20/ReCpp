// Group umbrella: arithmetic, vectors and sorting.
// Named numeric, not math, because rpp.math is already the module for math.h.
export module rpp.numeric;

export import rpp.math;
export import rpp.minmax;
export import rpp.vec;
export import rpp.sort;
